import styles from "./menu.css";

const Menu = () => {
  return /*htmL*/ `
    <div class="${styles.menu}">
        <h1>Our Menu</h1>
        <div class="${styles.menuItemsContainer}">
         
        </div>
    </div>
    `;
};

export default Menu;
